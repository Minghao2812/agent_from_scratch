---
description: 代码注释的 house style（Karpathy/nanochat 式）
globs: "**/*.py"
alwaysApply: false
---

# 注释 house style

本仓库是教学代码，鼓励大段设计注释（信息量是资产），但必须满足三条：

## 1. 仓库视角，不穿帮

注释以代码为主体，脱离授课语境也成立。禁止出现"这一课/这门课/下午的正题/知识点"
这类课程视角的表述，改为陈述该实现自身的目标与取舍。

```python
# ❌ 对这门课要讲的"MCP 协议长什么样"这个知识点是不必要的复杂度，所以没做
# ✅ 长连接复用引入的生命周期管理复杂度，与"演示 MCP 协议最小闭环"的目标不匹配，此处从简
```

## 2. 声明式陈述，不修辞

写"事实 + trade-off + 取舍原因"，不用反讽、假想敌式否定、带引号的修辞。
自嘲式精确描述可以留（如"粗暴截断"，同 nanoGPT 的 "poor man's data loader"）。
对外部项目的引用和具体数字（如 mini-swe-agent、SWE-bench 74%）是加分项，保留。

```python
# ❌ 多出来的东西不是"更花哨的框架"，而是三个真实存在于业界的设计
# ✅ 相比 naive/code agent 新增三个设计，均对应业界 deep research 产品的真实做法
```

## 3. 结构固定，位置得当

- module docstring 结构定死：一句话定位 → 设计要点（编号列表）→ 运行前/用法。
- 设计级论述放 module/function docstring 或段落分隔注释处，不埋进 if/elif 分支深处；
  行内只留一句结论 + 指路（如"见函数 docstring"）。
- 对照教学的两个文件允许重复注释（各自 self-contained），但可用"同 xxx.py"引用缩短。
